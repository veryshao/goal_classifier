# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Model weights and optimizer states
*.safetensors
optimizer.pt
rng_state.pth

# Study data — NEVER commit participant transcripts, labels, or derived outputs.
# Store externally (e.g. a shared encrypted drive or institutional data system).
data/
results/
evaluation/
evaluation_old/
evaluation_embed/
evaluation_bert_embed/

# Cached embeddings (large, reproducible from the API or local model)
embeddings_cache/
bert_embeddings_cache/
