# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Model weights and optimizer states
*.safetensors
optimizer.pt
rng_state.pth

# Study data — NEVER commit participant transcripts, labels, or derived outputs.
# Store externally (e.g. a shared encrypted drive or institutional data system).
data/
results/
evaluation/
evaluation_embed/
evaluation_with_pred/

# Cached embeddings (large, reproducible from the OpenAI API)
embeddings_cache/
